<div>
    {{ comments }}
    <h2>{{posts.title}}</h2>
    <h2>by {{posts.user.username}}</h2>
    <h3>{{posts.body}}</h3>
    {{#if posts.Comments}}
{{#each cleanComments}}
{{ this.body }}
:

{{/each}}
{{else}}
<h2>No commments...</h2>
    {{/if}}
</div>
{{#if logged_in}}
<div>
    <form id="comment-form">
        <textarea id="body-comment" style="height: 100px; resize: none;"></textarea>
        <button class="create-comment">Comment</button>
    </form>
</div>
{{/if}}
<div>
<a href="/">Back</a >
</div>
<script src="/js/comment.js"></script>